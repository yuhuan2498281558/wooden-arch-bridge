# 计划与进度

最后更新：2026-08-23

## 当前里程碑

- 已完成三节苗、五节苗骨架主链闭环：导师 SSA-XGBoost/CF-BPNN 输出矢跨比和五类根径，节点 Pilot 输出 `alpha/beta`，确定性几何计算节点及构件长度。本轮机器学习与后续小论文仍只研究五节苗牛头节点位置。

## 已完成

- 定位旧绘图问题：五节苗节点曾使用固定 `alpha=2/3`、`beta=1/4` 和固定像素偏移。
- 建立八点标注工具，支持缩放、拖拽、标签隐藏、JSON 恢复、批量复核、多跨和专用导出目录。
- 修复连续导入导致的桥名沿用问题，并增加按原图文件名恢复元数据的工具。
- 建立平行角度修正与法向抬高分离的节点几何计算。
- 建立对称标签预处理：保留左右历史观测，以左右均值生成 `design_target_alpha/beta`。
- 修正质量口径：历史不对称仅作诊断，不触发重标或训练排除。
- 完成 35 条样本、29 个独立分组的 v2 Pilot；35 条全部参训，6 条保留历史不对称诊断，0 条排除。
- v2 的 `alpha`、`beta` 当前均选择 Ridge；产物仍标记 `pilot_not_for_production`。
- 新一轮数据扩充至 59 条；补齐泰山桥 15.89 m、龙滩桥 23 m 跨长，并修正泰山桥误用的桥梁 ID/文件名。
- 建立零平弦特殊构造范围规则：合龙桥第 2 跨原始标注保留，以 `zero_inner_flat_chord` 排除，不视为标注错误。
- 完成 v3 Pilot：58 条、52 个独立分组参训，`alpha/beta` 仍均选择 Ridge；桥级宏 MAE 分别为 `0.07612`、`0.03506`，产物仍标记 `pilot_not_for_production`。
- 按三节苗三段投影各占净跨 `1/3` 的知识约束，从八点自动生成矢跨比、绝对矢高和设计斜弦角；斜弦角不与矢跨比重复入模。
- 完成 v4 五组特征消融：`alpha` 仍以净跨 Ridge 最稳，桥级宏 MAE `0.07612`；`beta` 采用净跨+矢跨比 Ridge，降至 `0.03279`。绝对矢高交互及多跨布局未取得稳定增益。
- 修复结构图纸、参数、结果、安全分析和方案优化等工作台标签与路由/侧栏菜单选中态不同步的问题。
- 将固定寿命占位值替换为解析 Weibull–Gamma 贝叶斯生存模型，增加在役年限、暴露、维护和防腐方案输入及预测区间展示。
- 完成贝叶斯寿命开源方案调研，形成 `docs/research/bayesian-service-life-open-source.md`。
- 移除前后端残留的 Ollama 开关和请求字段，自然语言设计固定走规则参数解析，并补充“数字+孔”表达测试。
- 网站现有可标注 3+5 编木拱桥已全部完成八点标注，共 103 份 JSON；通过网站详情中的原图文件名精确匹配补齐 43 条缺失净跨，并同步 16 条旧格式跨长字段。现在 103 条均同时具有 `span.clear_span_m` 和 `bridge.span_m`，两字段一致；飞云桥按词条正文“拱跨约19米”作为显式约值记录。
- 新增 `backfill_wbridge_spans.py`：默认干运行，应用时完整备份数据集并生成 `span_backfill_audit.csv`；最近复核为 `matched=0 existing=103 unresolved=0`。
- 训练加载复核：103 条全部完成预处理，102 条进入训练；仅合龙桥零平弦特殊构造排除 1 条，`missing_span_m=0`。
- Django 系统检查和前端生产构建此前已通过。
- 完成现状交付版《系统技术与使用说明》，覆盖系统框架、技术实现、现有功能、操作说明、部署验证、能力边界和待甲方确认事项。
- 完成 V1.1 Word 精排版：统一封面、目录、章节导航、页眉页脚、表格和架构/流程图，并完成 28 页逐页渲染检查。
- 修复训练方法学 P0：内层调参使用真实桥级宏 MAE，区分桥级与谱系分组级指标，并以 20 个隔离分组作独立内部留出，避免同一套 OOF 同时选优和报告。
- 完成 103 条标注的重新预处理：`needs_review=0`、缺净跨 0、零平弦特殊构造 1 条；102 条、96 个隔离分组进入 v5 训练。
- 完成 `five-miao-node-pilot-v5-independent-holdout`：`alpha` 为净跨 Ridge，独立留出桥级宏 MAE `0.09333`；`beta` 为净跨+矢跨比 Ridge，独立留出桥级宏 MAE `0.02991`。相同留出固定规则分别为 `0.13737`、`0.05768`，但模型仍为 Pilot。
- 完成节点模型最小接入：环境变量显式启用、受信任双产物加载、特征范围/OOD 检查、非法预测整组规则回退、响应元数据与 `/health` 状态；真实 v5 产物经 TestClient 调用验证。
- 完成 v6 小样本算法改进：冻结 v5 分区，在开发集比较 Ridge、Huber、ElasticNet、样条 Ridge、RBF-SVR、Gaussian Process 和浅层 Extra Trees，并以 LOGO 桥级宏 MAE和 q90 双门槛确认。七类候选均未达到替换条件，`alpha`、`beta` 继续保留 v5 Ridge；说明当前瓶颈主要在有效特征和样本差异。
- v6 产物新增标准化 k 近邻局部适用域、稀疏训练区域提示和 90% 经验误差区间；服务端保持硬范围越界整组回退。
- 从 `D:/qq/bridge-design.zip` 恢复导师两阶段模型：旧 pickle 转为 XGBoost 原生模型文件，CF-BPNN 转为只含数值数组的权重包，并记录原始文件和运行产物哈希、特征顺序、训练范围、输出范围及原有评估指标。
- 完成兼容式模型适配：`_predict` 主路径改为“SSA-XGBoost → CF-BPNN → 五节苗节点 Pilot → 确定性骨架几何”，规则只保留为明确标识的故障回退；`/health` 同时报告导师模型和节点模型状态。
- 前端预测结果页已展示 `alpha/beta`、导师参数模型版本、节点来源和完整算法链；2026-08-14 生产构建通过。
- 本地启动脚本在检测到受信任 v6 双产物时自动启用节点 Pilot；生产 Compose 继续默认关闭节点 Pilot，等待甲方验收后再显式配置。
- 算法、预处理、训练与接入测试最近一次通过：`75 passed`；实际 8001 服务已加载导师模型和 v6 节点双产物并完成接口数值验收。
- 完成 22 条 v6 高误差记录的原图人工复核：标注点位和原图匹配均无问题。高残差视为地形地势、墩台沉降、构件尺寸/施工误差、外界腐蚀和测量误差等综合影响形成的真实历史差异；未取得逐桥成因证据，不做单桥原因标签，也不排除或重标这些记录。
- 完成 v7 三层目标预处理：原始历史观测、历史左右均值代理和专家规范目标分字段保存；103 条均进入审计，102 条可用于历史代理研究，22 条标记为已确认历史差异，专家规范目标批准数为 0。
- 完成 v7 抗噪声基线：在冻结 v5 分区上比较 Ridge、Huber 和中位数分位回归；稳健候选未达到 `0.002` MAE 改善门槛，`alpha/beta` 均保留 Ridge，冻结留出桥级宏 MAE 为 `0.09333/0.02991`。研究产物禁止部署。
- 完成 v8 外节点两类结构门控实验：按 `alpha<0.5`/`alpha>=0.5` 划分前半区 35 条、后半区 67 条；其中 22 条位于阈值 ±0.03，6 条左右观测跨越不同半区。
- v8 采用 Logistic 前置分类和两套独立 Ridge 专家。开发集 LOGO 在已知真实类型时桥级宏 MAE 降至 `0.05299`，证明两类回归有价值；自动门控平衡准确率仅 `0.68834`，端到端 MAE `0.08457`、q90 `0.18278`，未优于 v6 的 `0.07258/0.14147`，因此未替换线上 v6。
- v8 独立内部留出自动门控 MAE 为 `0.08183`，虽低于 v6 的 `0.09333`，但留出未参与选模且开发集替换门槛未通过；产物标记 `research_structure_gate_not_for_deployment`。
- v8 阶段算法、预处理、训练与接入测试通过 `98 passed`（新增自然语言矢高解析、“系”计数、结构承重验算、安全分析联动与 Excel 命名与版式用例）；当前最新验证见后续直接坐标对照条目。
- 完成 v9 设计人员给定模式条件双专家：历史类型直接按 `design_target_alpha=0.5` 派生，不新增人工重标；前半区选择净跨 Ridge（alpha=1.0），后半区选择净跨 Ridge（alpha=1e-8）。开发集 LOGO 桥级宏 MAE `0.05296`、模式宏 MAE `0.05163`、q90 `0.10638`，相对 v6 单模型 `0.07258/0.07772/0.14147` 明显改善。
- v9 相对最强分区中位数的模式宏 MAE 仅由 `0.05459` 改善到 `0.05163`；排除边界或左右跨区样本后未持续达到 `0.002` 改善门槛，因此论文只能表述为设计模式条件预测，不能把净跨斜率或自动分型写成稳定泛化结论。
- 完成 v9 受控系统接入：前/后半区单选项位于自然语言交互区，默认后半区，文本明确模式时覆盖控件值；v9 仅覆盖 alpha，beta 继续使用 v6。接口、优化和缓存恢复均统一默认策略，Excel 和结果元数据保留选择来源与模型版本。
- 完成骨架立面方案图主图优先重构（Rev 3.2）：保留 B/C 三节苗拱顶牛头节点命名和 6 处顺弦轴牛头端头木块；图纸使用不可变 `design_inputs + result` 快照并校验模式/alpha 一致性。右栏只保留结构模式、净跨、矢高、alpha/beta、适用域/回退/Pilot状态、两条非模型说明和简化图签；详细参数移至结果界面。默认后半区真实产物完成渲染检查。
- 修复自然语言 `_extract_params` 未解析矢高的问题：现支持“矢高/拱高/起水高度/rise”，并补 `/chat` 集成用例。
- 修复 `start_bridge_algorithm.ps1` 在 Windows PowerShell 5.1 下因 UTF-8 无 BOM 导致节点模型自动启用条件不生效的问题；改为 UTF-8 BOM 后已实测脚本可直接自动启用 v6/v9。
- 将 n1/n2 的工程口径统一为“系”：一系三节苗含斜弦 2 根、平弦 1 根，一系五节苗含外斜弦 2 根、内斜弦 2 根、平弦 1 根。前端表单、Excel、结构图纸和自然语言解析同步改为“系”；图纸构件数量按 `n1、2×n1、2×n2、2×n2、n2` 展开。
- 结构图纸去除警示红：三节苗主拱改为绿色系，构件长度标注改深灰，适用域外推提示改琥珀色；图纸中不再使用 `#b91c1c/#991b1b/#e11d48`。
- 新增 `structural_check.py` 方案阶段承重验算并接入 `/analyze`：按 n1+n2 平面系均分桥面设计荷载，逐构件做压应力与欧拉屈曲双控包络验算，输出 DCR、能力/荷载倍数和三档年腐朽深度下的截面寿命代理。默认 14m 方案实测 `max_dcr=0.2697`、能力/荷载倍数 `3.71`；所有荷载与材料参数均显式标记为待确认假设。
- 新增 `safety_integration.py` 把安全分析三块串成一条链：结构包络 DCR → 当前暴露/维护/防腐情景的截面退化寿命 → Weibull–Gamma 贝叶斯寿命区间 → 取较不利寿命作综合结论；综合评分权重为结构 55%、截面寿命 25%、贝叶斯寿命 20%。`/analyze` 返回 `integrated_assessment.chain` 与 `governing_life_years`，前端显示“安全分析已联动”及控制寿命来源。
- 修正结构验算评分映射：不再用 `100-120×DCR` 线性扣分，改为按能力/荷载比分段插值；DCR=0.27（能力约 3.7 倍）结构分由 67.6 修正到 94.4。修正后默认遮蔽+防腐方案综合分 89.7/A，露天+无防腐 84.2/B。
- 修复“发送设计参数先跳图纸、短暂空白、再跳一次”的路由体验问题：原因是从 `/bridge/chat` 跳到 `/bridge/drawing` 时组件按路由重新挂载，`restoreBridgeState` 又清空 `imageSrc` 触发二次出图。现改为 `goToTab` 先 `router.replace` 再切 tab，并把 PNG 单独写入 sessionStorage 恢复，路由切换不再出现空白和二次跳转。
- 完成部署前全链路复验：算法测试 `97 passed`，Django check 通过，前端生产构建通过，Compose YAML 与必填环境变量 fail-closed 校验通过；`/health /predict /chat /visualize /analyze /optimize /export/excel /annotation` 冒烟全通过，后端验证码登录与 JWT 签发通过。发现并修复算法镜像缺 `scikit-learn/joblib` 依赖的部署阻断项；`.gitignore` 已忽略 `.env`、`.env.local` 和 `/scratch/`，避免服务器同步泄密与临时文件。
- 完成 `8.155.172.84` 服务器同步与验证：备份源码/镜像并重建 bridge-web/backend/algorithm/celery，未触碰其他系统容器；网站、登录、算法、结构验算、Excel 导出均通过。修复部署问题：服务器环境变量缺项、算法镜像 Debian apt 慢、Celery 结果依赖缺失及 Redis URL 组装错误。首次同步时节点 v6/v9 关闭，后续已按用户确认显式启用，当前状态见下文。
- 修正 Excel 与图纸五节苗斜弦口径：统一为“外斜弦（斜弦1）=A—Q1/Q4—D、内斜弦（斜弦2）=Q1—Q2/Q3—Q4”；Excel 补齐斜弦2长度和命名说明，图纸构件表与图例同步说明。服务器 bridge-algorithm 已同步重建并验证导出字段齐全。
- Excel 导出版式调整：删除独立“斜弦命名说明”行，命名说明并入“计算说明”列；表体改为四列（参数/数值/单位/计算说明），列宽 32/22/10/52 并设置横向打印居中，标题与表头水平居中；标题 18 号黑体，表头 11 号黑体，正文 11 号宋体，说明 10 号宋体灰字。服务器 bridge-algorithm 已同步。


- 新增测试集效果图脚本 `ml_pipeline/train/testset_effectiveness_plots.py`，基于 21 条独立留出测试集生成 4 组图：alpha 分区专家预测与误差、beta 平弦节点预测与误差、指标热力图、残差/物理误差分布；并输出完整指标表。测试集 alpha：固定规则 MAE 0.13548、v6 单模型 0.09049、v6 区间约束 0.08426、分区中位数 0.06154、v9 分区专家 0.06037（R²=0.651）；前半区 v9 MAE 0.06067，后半区 0.06022。beta：固定规则 0.05733、开发集中位数 0.05582、v6 模型 0.02892（R²=0.620），平均平弦节点误差 0.209 m。结果输出至 `机器学习结果/testset_effectiveness_plots_v1` 和 `scratch/testset_effectiveness_plots_v1`。
- 新增消融结果绘图脚本 `ml_pipeline/train/preprocessing_ablation_plots.py`，生成 5 张论文用图：基线/逐项消融、四类预处理贡献、平行角度修正诊断、残差与裁剪消融、OOF 预测散点与误差分布；PNG/PDF 同步至结果目录和 `scratch/preprocessing_ablation_v3/figures`。
- 完成数据预处理消融实验 `ml_pipeline/train/preprocessing_ablation.py`：LOOGO + 固定 Ridge(1e-8) 逐项关闭预处理。完整流程 alpha/beta 桥级宏 MAE 0.07671/0.03008，优于固定规则 0.12393/0.05079 和训练中位数 0.09760/0.05005；去掉零平弦范围控制使 beta 从 0.03008 升至 0.03114，证明特殊构造过滤有效；左右分别建模未带来增益且破坏对称性；平行角度修正的 alpha 标签平均移动 0.0457，价值在物理语义；净跨补全将可用样本从 59 条/53 组提升到 102 条/96 组，beta 宏 MAE 由 0.03240 降至 0.03008。结果已输出至 `机器学习结果/preprocessing_ablation_v3` 和 `scratch/preprocessing_ablation_v3`。
- 补做原始标签与完整预处理的严格配对实验：冻结 102 条样本、v5 分区、特征、分组和 Ridge(alpha=1e-8)，只改变标签构造。alpha 开发集桥级宏 MAE `0.07569→0.07258`（降低 4.1%），冻结内部留出 `0.09649→0.09333`（降低 3.3%）；配对 bootstrap 95% 区间跨 0，只能表述为小幅一致改善。beta 数值等价，不能声称对称均值本身带来精度增益。实验产物已保存到 `preprocessing_strict_control_v1`，9 组论文候选图按正文/补充材料整理至桌面 `机器学习结果/论文图片备选_20260820`。
- 完成原始像素/坐标直接预测对照：冻结同一 102 条样本、v5 分区和 Ridge，比较原始像素、图像归一化坐标、桥轴局部坐标与知识参数化 `alpha/beta`。冻结留出上原始像素/图像归一化节点误差为 `2.9815/2.7568 m`；最强局部坐标基线复现历史观测为 `0.5828 m`，与知识参数化 `0.6089 m` 的配对差异区间跨 0，但对对称设计代理为 `0.6706 m`，显著差于知识参数化 `0.4660 m`，且 `95.2%` 样本触发偏轴/非对称几何警告。实验脚本为 `ml_pipeline/train/direct_coordinate_baseline.py`，结果位于 `scratch/direct_coordinate_baseline_v2`；2026-08-23 全量算法测试 `100 passed`。
- 完成 v10 显式模式部分共享 alpha 实验：冻结 102 条合格样本及 v5 分区，比较分区中位数、共享斜率、部分共享斜率和 v9 独立专家。开发集模式宏 MAE 为 `0.05459/0.05166/0.05166/0.05163`；M2 选择交互惩罚 `1000`，实质收缩到共享斜率。按 `0.002` 容差优先 M1，但未达到替换 v9 的实质改善门槛，因此只保留离线研究结果，不改线上模型；历史留出 M1 模式宏 MAE `0.05929` 仅作描述。新增 5 项测试后全量算法测试 `105 passed`。
- 完成 v11 倾斜角消融：比较共享净跨、共享倾斜角、净跨+倾斜角、净跨+矢跨比和 v9 独立专家。开发集模式宏 MAE 为 `0.05166/0.05722/0.05254/0.05260/0.05163`；净跨+角度相对净跨的桥级配对 MAE 增加 `0.00074`，95% bootstrap CI `[0.00043,0.00111]`，仅 `10/76` 个桥组更优。角度未通过替换门槛，不改线上模型；新增 6 项测试后全量算法测试 `111 passed`。
- 使用说明与算法提示中的“导师模型/导师参数模型”表述已统一改为“参数预测模型”或保留用户使用的“xgboost+cf-bpnn模型”；文档 PDF 已重出，服务器 bridge-algorithm 已同步重建。
- 使用说明按用户修改版继续修订：保留用户删减内容，补充当前部署实例和受控登录说明；部署章节改为“当前实例优先、自部署留作迁移参考”，Word 目录已刷新并重新导出 PDF（22 页）。项目记忆不保存具体账号或密码。
- 使用说明更新至 V1.4：同步安全分析“结构包络验算→截面退化寿命→贝叶斯寿命区间→综合结论”四步联动、55/25/20 加权评分及分段映射修正，并同步 Excel 四列版式、服务器节点 Pilot 启用状态和 98 项测试；Word 目录域已刷新并导出 PDF（23 页）。
- 服务器节点 Pilot 已按用户确认启用：`.env` 增加 `BRIDGE_NODE_MODEL_ENABLED=true`、`BRIDGE_NODE_DESIGN_MODE_MODEL_ENABLED=true`，只重建 bridge-algorithm。`/health` 显示 v6/v9 均 available；`/predict` 后半区 `alpha=0.574967`、前半区 `alpha=0.410339`，来源 `designer_mode_pilot`。完整参数（含总长 42m）前半区聊天出图正常；输入使 `rise_span` 越出 v9 训练范围时按设计回退 v6/规则并明确标注。








## 进行中

- 已去掉外斜弦 α 派生模式在 0.5 处的硬切：`derived_structure_mode` 使用 `STRUCTURE_BOUNDARY_BAND=0.03`（远济均值 0.503、22/103 贴边）。已提交后半区内对咏归式向下收缩做保守 2/3 混合（α≥0.70 仅 7 条，不另拟合专家；北涧/田地主体预测不抬到 0.75）。岚下前半区低尾（2026-08-25 重标 0.277/0.187、均值 0.232）为孤立点，v9 专家约 0.427 落在主体，不混合、不删样本。在线 v9 仍由设计人员指定前/后半区。不换 Ridge。云端无 103 JSON，留出需本地复跑 `design_mode_alpha`。
- 收敛五节苗节点论文实验口径：v9 定位为设计人员给定模式的条件预测；主对比写作采用“直接坐标回归与知识约束参数化”，同时报告历史现状复现和对称设计代理，不把当前结果写成“预处理显著提高平均精度”。
- v10 结果支持把共享斜率作为小样本简化候选，但不支持宣称部分共享显著优于独立专家；正式替换需新增、从未查看过的桥梁或测绘谱系外部组，并预先冻结 M1 与 v9 的比较门槛。
- v11 已排除“仅改变矢跨比为倾斜角表示”作为主要改进方向；下一步优先补充设计时可获得的构造亚型和专家规范目标，不再继续堆叠角度/矢跨比变换。
- 当前服务器已显式启用 v6/v9 Pilot；继续完成正式账号下的浏览器端验收、外部验证和论文实验冻结。仓库默认配置仍关闭节点 Pilot，其他环境必须显式启用。
- 补充独立外部数据和分区中位数对照，判断双 Ridge 相对“仅类型先验”的小幅增益能否稳定复现。

## 待办

0b. 【审计基线，需决策】将 `bridge_algorithm_service/`、`ml_pipeline/`、`docker-compose.bridge.yml`、`docker_env/bridge/`、`docs/` 等未跟踪目录纳入 Git 建立基线；修复 CI 后推送验证（`.github/workflows/playwright-i18n.yml` 已改为 npm + Django check + 算法测试）。
1. 在甲方或正式测试账号下完成“导师模型 → `alpha/beta` → `Q1—Q4` → 3+5 骨架图”的浏览器端验收；当前接口、自动化测试和前端生产构建已通过，本地浏览器因无登录态停在登录页。
2. 对当前已启用的服务器节点 Pilot 完成正式账号验收，确认 v6/v9 来源、OOD/非法预测回退、图纸和 Excel 元数据一致。
4. 建立桥梁信息库资料关联：纵剖图用于节点比例，俯视图和现场照片作为可追溯构造资料；暂不扩展成其他构件预测数据集。
5. 扩充候选桥数据和双人复标子集，估计标注者间误差；不得因历史结构不对称而强制改点。
6. 将专家规范目标模板保留为可选校准工作，不阻塞历史智慧复现型 Pilot；若未来提高到工程定型模型，再要求专家目标和外部验证。
7. 按作者许可记录网页引用和原图展示清单，冻结数据字典、训练集版本和论文实验方案。
8. 小论文只围绕五节苗牛头节点：按固定比例、训练中位数、Ridge/Huber及后续候选模型完成对照实验，不把矢跨比或根径包装成本轮贡献。
9. 【已决策】前半区/后半区由设计人员显式选择；v8 自动分类只保留为论文消融。仓库默认关闭 v9，当前服务器已显式启用；下一步用正式账号点验选择控件、结果元数据、图纸和 Excel。

## 阻塞与风险

- 网站作者已允许公开图纸下载和数据分析；论文直接展示原图超过 10 张或派生数据公开发布的边界仍需再次确认。
- 当前有 102 条标准桥型可训练记录；足以开展骨架节点历史规律 Pilot，但多跨布局仍只覆盖少数桥梁/谱系，尚不足以支持工程定型或无条件泛化结论。
- 尚无独立外部测试集，也尚未估计双人标注误差。
- 两类结构假设对回归有效，但 0.5 附近存在连续过渡；派生标签已用 `STRUCTURE_BOUNDARY_BAND` 避免刀切。现有总体参数仍不足以可靠自动判类；错误路由会抵消双专家收益。在线合同保持设计人员给定模式。
- 导师两阶段模型已从用户备份恢复并数值复现，但旧阶段一产物记录的独立测试 `R²=-0.38868`、`MAE=0.02074`，不能因已接入就宣称具备充分泛化能力；原 SSA 搜索过程的精确版本也仍需结合导师原始训练记录确认。
- 寿命模型的先验、情景因子和维修/更换终点尚未由项目数据或专家批准校准，当前结果只能用于方案筛选，不能作为安全鉴定或报废依据。

## 下一接手点

- 先读取 `Prompt.md` 和 `Implement.md`。派生模式门控已改为边界带；后半区咏归式高尾为保守 2/3 混合；岚下前半区低尾经调查不混合。下一步完成正式登录态浏览器验收、独立外部/专家目标验证和论文实验冻结。本地有数据集时应复跑 v9 留出并记录远济、咏归、岚下行。
- 系统集成保持“模型可选、规则可回退”，输出定位为传统骨架智慧复现型 Pilot，不扩展到其他构件或施工级设计。
- 论文与模型改进可使用历史代理研究现存骨架规律，但必须明确其历史数据语义；专家规范目标作为后续提高证据等级的可选阶段。22 条高误差记录已经复核有效，不再重复按错标处理。
