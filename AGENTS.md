# 项目协作说明

## 项目记忆路由

- 涉及项目目标、功能范围、验收标准、约束或工具偏好时，读取 `Prompt.md`。
- 涉及任务规划、继续工作、进度或下一步时，读取 `Plan.md`。
- 涉及代码修改、架构、模块边界、数据流或实现决策时，读取 `Implement.md`，再检查相关代码。
- 涉及环境配置、依赖、运行、测试、构建或排障时，读取 `Documentation.md`。
- 同时涉及多个方面时，只读取对应文件，不必每次加载全部项目记忆。

## 工作与验证规则

- 代码、配置及专项文档是最终事实来源；记忆与代码冲突时先核实代码，再更新记忆。
- 修改节点几何或训练逻辑时，至少运行 `python -m pytest bridge_algorithm_service/tests -q`。
- 修改标注工具时，除脚本语法检查外，还应在浏览器验证载入 JSON、匹配原图、八点保留、缩放和平移。
- 修改训练流水线时，必须保持按 `split_group_key` 分组验证，禁止同一桥或同一测绘谱系跨训练/测试折。
- 不覆盖用户已有改动，不使用破坏性 Git 命令；当前工作区可能长期存在未提交修改。
- 完成任务后，只更新本次确认且仍有效的记忆；替换过时状态，不追加重复流水账。

## 可视化交付规则

- 当前 Codex 桌面端不把 Cursor 专用的 `.canvas.tsx` 文件作为可正常显示的用户交付物；不要再将 `.cursor/projects/*/canvases/` 下的文件称为图片或仅发送其链接。
- 需要图表、数据面板或交互式可视化时，优先使用已安装的 `visualize:visualize` 插件直接在对话中呈现。
- 若对话内可视化不可用，则改用聊天中的清晰文字，或生成普通 PNG 并用绝对路径嵌入；交付前必须确认用户能够直接查看。

## 维护约束

- 不记录密钥、令牌、密码、个人数据或 `.env` 的具体值。
- 无法确认的信息标记为“待确认”，不得写成事实。
- 原始 README、专项 README 和 `docs/五节苗牛头节点预测研发与论文路线.md` 保持权威地位；记忆只保存摘要和链接。
- 五个记忆文件均应保持 UTF-8，单文件尽量不超过 200 行。
