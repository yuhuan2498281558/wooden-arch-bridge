# 项目需求与偏好

> 保存已确认且相对稳定的需求、约束和本项目专属偏好；当前进度写入 `Plan.md`。

## 项目目标

- 在 Django-Vue3-Admin 基础上建设木拱桥智能设计系统。
- 以复现传统木拱桥营造智慧和建设可追溯信息库为核心，根据用户需求生成三节苗、五节苗骨架方案。
- 当前智能设计最终输出三节苗、五节苗的构件长度与根径，以及五节苗四个牛头节点 `Q1—Q4` 的位置；本轮新增机器学习只研究牛头节点位置。
- 补齐五节苗四个牛头节点 `Q1—Q4` 的位置预测，替代原先固定 `alpha=2/3`、`beta=1/4` 和固定像素偏移的临时规则。
- 从现存木拱桥测绘图建立可追溯数据集，完成小样本算法、系统集成和后续论文研究。

## 目标用户与场景

- 面向木拱桥研究、参数设计和图纸复核人员。
- 用户输入总体设计参数；系统预测三节苗几何及五节苗节点，输出中心对称、可构造的智能设计图。
- 标注人员从历史纵剖面图中按真实现状标注节点；历史桥本身不要求左右对称。

## 已确认需求

- 八点标注定义：三节苗锚点 `A、B、C、D`，五节苗节点 `Q1、Q2、Q3、Q4`；工程图纸命名中 B、C 保留为“三节苗拱顶牛头节点”，Q1/Q4 为外牛头节点，Q2/Q3 为内牛头节点，数据标识不变。
- 用无量纲比例预测节点：外侧比例 `alpha`、内侧比例 `beta`，而不是直接学习像素坐标。
- 外节点采用两种设计模式：位于对应三节苗斜弦前半区 `alpha<0.5` 与后半区 `alpha>=0.5`。不再增加人工重标。历史训练类型不在 0.5 处硬切：`|α-0.5|≤STRUCTURE_BOUNDARY_BAND(0.03)` 或左右并非两侧都明显同一半区时标为未提交，评估走连续/v6 式预测；远离 0.5 仍按前/后半区。已提交后半区内，专家若相对训练中位数明显向下收缩（咏归式），则与固定规则 2/3 取较高值；不新增设计人员控件。岚下前半区低尾为核实后的孤立留出点，不因误差删除，也不对前半区主体做下拉混合。设计人员在自然语言交互区选择前/后半区，未选择时默认后半区，文本中明确写出的模式优先；自动分类只保留为论文消融。
- 历史左右观测必须原样保留；设计标签采用左右均值，生成中心对称结构。
- 五节苗平弦长度为零、两根内斜弦直接相接的桥型保留原始记录，但不纳入当前标准五节苗模型训练。
- 地形、墩台沉降、构件尺寸、施工和测量误差造成的不对称是有效历史观测，不得仅因不对称超过阈值而排除或要求重标。
- 模型残差较大不等于标注错误；经原图复核确认无误的高误差样本继续保留为历史现状观测，不得为了贴合模型预测而改点或删除。
- 五节苗外斜弦方向固定为与对应三节苗斜弦平行；构件厚度造成的法向起点抬高单独记录，第一版不作为位置预测目标。
- 三节苗左斜弦投影、中间平弦和右斜弦投影在智能设计中各占净跨 `1/3`；矢跨比与斜弦角按该约束互相确定，不作为重复独立特征。
- 多跨桥按“一跨一个样本/JSON”处理；同桥各跨不得跨训练和测试集合。
- 标注 JSON、CSV 按桥名和跨序号区分，并允许指定专用导出目录且不覆盖同名旧文件。
- 标注工具需要缩放、拖拽平移、节点字母隐藏/显示、JSON 批量复核队列和原图严格匹配。
- 后续系统集成必须保留可行域约束、训练范围/OOD 检查、规则回退、模型来源及版本信息。
- 矢跨比及三节苗、五节苗根径属于导师论文已经解决的既有能力：当前系统已从用户提供的原始备份恢复 SSA-XGBoost 与 CF-BPNN 运行链路；XGBoost 转为原生模型文件加载，CF-BPNN 继续按原权重矩阵直接推理。本轮不重新训练、不改算法。
- 骨架输出范围固定为三节苗和五节苗；桥面、横向联系、榫卯细部、栏杆、廊屋及其他附属构件不进入当前预测和优化目标。
- 俯视图、现场照片等资料可进入桥梁信息库，用于解释和追溯传统构造，但不是当前牛头节点模型的必备训练输入。
- 后续形成一篇围绕小样本、知识约束、对称预处理和工程集成的论文。
- 论文主对比采用“直接坐标回归与知识约束参数化”，不得简化成“预处理显著提高平均精度”：原始像素、图像归一化坐标和桥轴局部坐标用于层级基线，必须同时报告历史现状复现与对称设计代理两个共同评估终点；对称设计代理不是独立专家真值。

## 验收标准

- 节点满足 `0 < alpha < 1`、`0 < beta < 0.5`，设计输出左右中心对称。
- 系统能够输出三节苗、五节苗骨架的构件长度、根径和五节苗牛头节点位置，并记录结果来源、模型/规则版本及适用范围。
- 原始点、左右比例、不对称度、法向偏移、桥/跨身份和来源信息可追溯。
- `quality=low`、几何越界、缺点或跨信息非法的样本不参与训练；历史不对称本身不构成排除理由。
- 数据划分使用桥梁/谱系级分组；指标以桥级宏平均误差为主，并与固定规则和训练中位数比较。
- 未经专家复核和外部验证的模型必须标记为 Pilot，不直接作为工程定型模型。
- 设计模式条件双专家必须以设计人员预先给定 `front_half/back_half` 为在线合同，按桥/谱系分组验证，并与分区中位数、原 v6 和区间约束 v6 比较；论文不得把目标派生历史类型表述为独立专家标注或自动结构识别。
- 直接坐标与知识参数化对比必须冻结相同样本和 v5 分区，主结论优先比较最强桥轴局部坐标基线，不得只用天然受图纸裁切影响的原始像素基线放大效果。

## 约束与非目标

- 五节苗节点预测是相对导师论文既有矢跨比/根径模型新增的独立几何补全阶段，也是本轮小论文唯一的机器学习研究对象。
- 导师模型现已作为 `_predict` 的第一阶段接入；本地规则仅在模型被显式关闭、产物缺失或推理失败时作为兼容回退。响应必须清楚返回模型状态、版本、阶段来源和回退原因。
- 当前系统不以完整桥梁、施工图、三维层位、榫卯或其他构件设计为目标；骨架结果属于历史数据与知识规则驱动的方案生成，不替代结构验算和施工设计。
- 第一版不预测木材厚度，也不把历史变形量直接复制到新桥设计。
- 历史现状观测与新桥规范化设计目标必须区分：沉降、腐蚀、施工尺寸偏差和测量误差等综合影响可进入观测噪声/不确定性分析，但不能在缺少逐桥成因证据时直接解释为设计规律。
- 专家规范目标层保留为后续校准和提升证据等级的可选路径，不作为当前“古桥智慧复现型骨架 Pilot”生成与联调的前置条件。
- 当前特征消融已覆盖净跨、矢跨比、绝对矢高交互和多跨布局，但不据此宣称复杂模型已具备充分泛化能力。
- 网站作者已书面确认公开图纸可下载并用于数据分析；仅使用图纸数据时按网页文献格式清晰标注来源。论文若直接展示网站原图，应控制在许可范围内；超过 10 张或用途变化时再次联系作者确认。
- 不公开或记录数据库密码、BIMFACE 密钥及其他环境秘密。

## 项目工具偏好

- 优先使用可解释的小样本基线和知识约束残差学习；复杂模型必须通过分组嵌套验证证明增益。
- 图纸标注以浏览器本地处理为主，原图不上传服务端。
- 文件名、桥名、跨序号和谱系 ID 应保持稳定、可追溯。

## 待确认问题

- 派生数据公开发布的具体边界，以及论文最终直接展示图纸数量。
- 原论文 130+ 座桥数据能否取得并与网站数据去重合并。
- 五节苗节点正式模型最终可用特征及其数据完整度；不得为提高节点指标而擅自重做导师论文已经解决的矢跨比和根径模型。
- SSA 训练产物的精确搜索来源仍待确认：备份同时包含原始 MATLAB SSA 和后期 Python 近似搜索脚本，现有模型文件本身不能证明当时采用了哪一种搜索过程；该问题不影响已保存模型的数值复现，但影响论文方法溯源表述。
- 构件厚度进入正式设计时采用专家规则、构件直径推导还是独立模型。
